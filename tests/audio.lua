describe("Audio", function()
  describe("DividingTimer", function()
    setup(function()
      DividingTimer = require("gameboy/audio/dividing_timer")
    end)
    it("can be created", function()
      timer = DividingTimer:new()
      assert.are_not_same(timer, nil)
    end)
  end)
end)